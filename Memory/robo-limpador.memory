# robo-limpador — Contexto do Projeto

## Identificação
- **Caminho:** `C:\Users\gabri\Área de Trabalho\robo-limpador`
- **Stack:** Python 3, `numpy` + `matplotlib` (via `venv` + `requirements.txt`)
- **Tipo:** Atividade Avaliativa de IA — Aspirador de Pó Automático (APA): agente reativo simples + agente baseado em objetivos. Mundo real é **6x6 com bordas-parede** (linha/coluna 1 e 6 = parede, área limpável é o 4x4 central). Enunciado oficial em `docs/AspiradorDePó.pdf`. **Etapa atual: só o ambiente (matriz), sem lógica de agente ainda.**

---

## Arquitetura

- `docs/AspiradorDePó.pdf` — enunciado oficial da atividade avaliativa (fonte da verdade para requisitos futuros)
- `robo_limpador/ambiente.py` — `criar_sala(tamanho_total=6)`: matriz 6x6 onde a borda é `PAREDE=1` e o quadrado interno 4x4 é `LIMPO=0` com 3 a 7 células `SUJO=2` sorteadas (estado inicial de episódio, não re-sorteia por rodada); `checkObj(sala)`: retorna `1` se há `SUJO`, `0` se limpa — pronta para o agente baseado em objetivos (próxima etapa). Constantes: `TAMANHO_TOTAL=6`, `TAMANHO_UTIL=4`, `LIMPO=0`, `PAREDE=1`, `SUJO=2`, `MIN_SUJEIRAS=3`, `MAX_SUJEIRAS=7`
- `robo_limpador/visualizacao.py` — `exibir(matriz, pos_x=None, pos_y=None)`: mostra a matriz com `matplotlib` (adaptado do modelo do enunciado); posição do agente é opcional pois o agente ainda não existe
- `main.py` — ponto de entrada, cria a sala e chama `exibir()` (sem loop, sem agente)
- **Pendente (próxima etapa, fora desta):** ver checklist completo em `PROXIMOS_PASSOS.md` — `robo_limpador/agente.py` (`funcaoMapear`, `agenteReativoSimples`, `agenteObjetivo`, contador `pontos`), loop de simulação, e `RESPOSTAS.md` com as duas perguntas discursivas do enunciado
- Separação intencional (via skill `arquitetura-projeto`): ambiente / visualização ficam em módulos distintos para o agente entrar depois sem reescrever essas partes
- `.claude/` — skills (`arquitetura-projeto`, `otimizacao-codigo`, `frontend-design`) e agentes (`depurador`, `descritor-pr`, `documentador`, `especialista-banco`, `revisor-codigo`) instalados neste projeto
- `claude/` — clone do repositório https://github.com/Bugmenn/claude (fonte original das skills/agentes acima), mantido como pasta comum (sem `.git` interno)

---

## Padrões de Desenvolvimento

- Nomes de módulos/funções/variáveis em português, consistente com o restante do projeto (e com os nomes exigidos pelo enunciado: `agenteReativoSimples`, `agenteObjetivo`, `checkObj`, `funcaoMapear`, `pontos`, `posAPAx`/`posAPAy`)
- Módulos com responsabilidade única (estado do ambiente ≠ exibição ≠ lógica do agente)
- `checkObj` foi colocada em `ambiente.py` (não no agente) por operar só sobre o estado da sala — comentário no código explica que é para uso futuro do agente

---

## Pitfalls / Cuidados

Nenhum identificado até o momento.

---

## Registro de Atividades Recentes

### [2026-09-14] Configuração inicial de memória e diretrizes
**O que foi feito:** Clonado o repositório de configuração (Bugmenn/claude) para dentro do projeto e aplicado o setup descrito em `config claude.md`: criada a pasta `Memory/` com `diretrizes.md` e este arquivo `.memory`.
**Arquivos alterados:**
- `Memory/diretrizes.md` *(novo)*
- `Memory/robo-limpador.memory` *(novo)*
- `claude/` *(novo, clone externo)*

### [2026-09-14] Primeira etapa do robô limpador — matriz 6x6 com sujeira aleatória
**O que foi feito:** Criado o ambiente inicial da simulação em Python: matriz 6x6 que a cada rodada sorteia quais casas ficam sujas (probabilidade fixa por célula), com impressão no console. Estrutura separada em módulos (ambiente / visualização / loop de execução) seguindo a skill `arquitetura-projeto`, para o agente/IA ser plugado depois sem reescrever essas partes. Testado rodando `main.py` via venv — 5 rodadas com padrões de sujeira diferentes entre si.
**Arquivos alterados:**
- `robo_limpador/__init__.py` *(novo)*
- `robo_limpador/ambiente.py` *(novo)* — classe `Ambiente` com grade 6x6 e `sortear_sujeira()`
- `robo_limpador/visualizacao.py` *(novo)* — `imprimir_grade()`
- `robo_limpador/simulacao.py` *(novo)* — `executar(numero_rodadas)`
- `main.py` *(novo)* — ponto de entrada
- `requirements.txt` *(novo)* — vazio por enquanto (só stdlib)
- `.venv/` *(novo, ambiente virtual, ignorado pelo git)*

### [2026-09-16] Pivô para a atividade avaliativa oficial (APA) — só o ambiente 4x4
**O que foi feito:** Usuário trouxe o enunciado oficial da atividade (aspirador de pó automático: agente reativo simples + agente baseado em objetivos, mundo 4x4). Isso substitui a simulação 6x6 genérica anterior. Nesta etapa, a pedido do usuário, implementei só o ambiente/matriz — a lógica do agente fica para depois. Planejamento passou por plan mode, incluindo revisão via skills `arquitetura-projeto` e `otimizacao-codigo` antes de codar (conclusão: manter a mesma separação de módulos do projeto anterior, sem necessidade de numpy no sorteio da matriz, `checkObj` cabe em `ambiente.py` com comentário explicando que é para o agente futuro). Testado com backend não interativo do matplotlib (`MPLBACKEND=Agg`) — `criar_sala()`, `checkObj()` e `exibir()` funcionando sem erro.
**Arquivos alterados:**
- `docs/AspiradorDePó.pdf` *(novo)* — enunciado oficial, copiado de `C:\Users\gabri\Downloads\AspiradorDePó.pdf`
- `robo_limpador/ambiente.py` — reescrito: `criar_sala(tamanho=4, probabilidade=0.3)` e `checkObj(sala)` (antes era a classe `Ambiente` 6x6 com `sortear_sujeira()`)
- `robo_limpador/visualizacao.py` — reescrito: `exibir(matriz, pos_x=None, pos_y=None)` com `matplotlib` (antes era `imprimir_grade()` com print no console)
- Removido: `robo_limpador/simulacao.py` — não se aplica mais (não há loop de rodadas nesta etapa)
- `main.py` — reescrito: só cria a sala e chama `exibir()`
- `requirements.txt` — agora lista `numpy` e `matplotlib` (antes vazio)

### [2026-09-18] Correção do mundo — 6x6 com bordas-parede, área útil 4x4
**O que foi feito:** Usuário corrigiu o entendimento do ambiente: a matriz é 6x6, mas só o quadrado central 4x4 é sala limpável — linha 1, linha 6, coluna 1 e coluna 6 (1-indexado) são paredes. Decidido em plan mode: representar tudo numa única matriz 6x6 com valor especial `PAREDE=-1` (em vez de matriz de sujeira + máscara separada), e deixar o `cmap='gray'` do matplotlib distinguir os 3 valores automaticamente (sem colormap customizado). `checkObj` não precisou mudar (paredes nunca valem `1`). Testado com backend não interativo — bordas saem `-1`, sujeira só sorteada nas células internas.
**Arquivos alterados:**
- `robo_limpador/ambiente.py` — reescrito: `criar_sala()` agora gera 6x6 com bordas `PAREDE` e sorteia sujeira só no 4x4 interno (antes gerava uma matriz 4x4 pura, sem parede)

### [2026-09-18] Codificação da matriz e quantidade de sujeiras
**O que foi feito:** Por pedido do usuário: cada sala tem de 3 a 7 sujeiras (sorteadas com `random.sample` no 4x4 interno, em vez de probabilidade por célula — parâmetro `probabilidade` removido). Codificação da matriz redefinida: `LIMPO=0`, `PAREDE=1`, `SUJO=2` (antes parede era -1 e sujo era 1). `checkObj` agora procura `SUJO` (2) em vez de `1` — mudança necessária, senão as paredes (1) seriam lidas como sujeira. Validado com 2000 salas: contagem sempre entre 3 e 7. Zip do projeto sem arquivos do Claude gerado em `C:\Users\gabri\Área de Trabalho\robo-limpador.zip` (antes desta mudança de codificação — regerar se for enviar de novo).
**Arquivos alterados:**
- `robo_limpador/ambiente.py` — novas constantes `LIMPO/PAREDE/SUJO/MIN_SUJEIRAS/MAX_SUJEIRAS`, `criar_sala` sem `probabilidade`, `checkObj` procura `SUJO`
- `CLAUDE.md`, `PROXIMOS_PASSOS.md`, `Memory/robo-limpador.memory` — codificação e regra de 3 a 7 sujeiras documentadas
